# Notes

This is a practice with Node

```
Options:
--help Show help [boolean]
--version Show version number [boolean]
-b, --base It is the base of the multiplication table [number] [required]
-u, --until Number up to where you want the table [number] [default: 10]
-l, --list Show table in console [boolean] [default: false]
```
